
--colorizer
-- Attaches to every FileType mode
require 'colorizer'.setup()
--tree facility
require 'FTerm'.setup({
  border     = 'double',
  dimensions = {
    height = 0.9,
    width = 0.9,
  },
})
